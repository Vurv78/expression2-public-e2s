@persist DEFINED
#defines FCVAR print("Hello world!")
#defines DEF DEFINED=1

#include "defonself"
definesInit( getCode(  ) ,   "defines"  )

"DEF"
if(DEFINED){
    # Use this to make sure stuff doesn't happen twice.
    print("LOL This is defined finally!")   
}
"FCVAR"
